<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <include layout="@layout/layout_title"/>
    <android.support.v4.view.ViewPager
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:id="@+id/view_pager">
    </android.support.v4.view.ViewPager>
    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:id="@+id/radio_group">
        <RadioButton
            android:id="@+id/useful_frame"
            android:text="@string/useful_frame"
            android:drawableTop="@drawable/useful_frame_drawable_selector"
            style="@style/radioButton"/>
        <RadioButton
            android:id="@+id/the_third"
            android:text="@string/the_third"
            android:drawableTop="@drawable/the_third_drawable_selector"
            style="@style/radioButton"/>
        <RadioButton
            android:id="@+id/custom_view"
            android:text="@string/custom_view"
            android:drawableTop="@drawable/custom_view_drawable_selector"
            style="@style/radioButton"/>
        <RadioButton
            android:id="@+id/other"
            android:text="@string/other"
            android:drawableTop="@drawable/other_drawable_selector"
            style="@style/radioButton"/>
    </RadioGroup>
</LinearLayout>
